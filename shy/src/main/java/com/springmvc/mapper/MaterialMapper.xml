<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.springmvc.mapper.MaterialMapper" >
    <resultMap id="MaterialMap" type="com.springmvc.entity.Material" >
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <id column="id" property="id" jdbcType="INTEGER" />
        <result column="mname" property="mname" jdbcType="VARCHAR" />
        <result column="mdate" property="mdate" jdbcType="TIMESTAMP" />
        <result column="price" property="price" jdbcType="OTHER" />
        <result column="number" property="number" jdbcType="INTEGER" />
        <result column="total" property="total" jdbcType="OTHER" />
        <result column="mstatus" property="mstatus" jdbcType="INTEGER"/>
        <result column="master" property="master" jdbcType="VARCHAR" />
        <result column="endaddress" property="endaddress" jdbcType="VARCHAR" />
        <result column="ps" property="ps" jdbcType="VARCHAR" />
        <result column="del" property="del" jdbcType="INTEGER"/>

    </resultMap>


    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="MaterialMap">
        select
            id,mname,mdate,price,number,total,mstatus,endaddress,ps
        from material
        limit #{offset}, #{limit}

    </select>
    <!--查询全部用户订单信息-->
    <select id="queryMaterialAll" resultMap="MaterialMap">
        select
            *
        from material
        where del=1
    </select>

    <!--查询全部用户订单信息-->
    <select id="queryMaterialByInf" resultMap="MaterialMap">
        select
        *
        from material
        <where>
            <if test="mname != null and mname != ''">
                mname = #{mname}
            </if>
            <if test="mdate != null ">
                and mdate = #{mdate}
            </if>
            <if test="mstatus != null">
                and mstatus = #{mstatus}
            </if>
            and del=1
        </where>
    </select>

    <!--新增所有列-->
    <insert id="addMaterial" keyProperty="id" useGeneratedKeys="true" >
        insert into material(id,mname,mdate,price,number,total,mstatus,endaddress,ps)
        values (#{id},#{mname}, #{mdate}, #{price}, #{number}, #{total}, #{mstatus}, #{endaddress}, #{ps})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update material
        <set>
            <if test="mname != null and mname != ''">
                mname = #{mname},
            </if>
            <if test="mdate != null">
                mdate = #{mdate},
            </if>
            <if test="price != null">
                price = #{price},
            </if>
            <if test="number != null">
                number = #{number},
            </if>
            <if test="total != null">
                total = #{total},
            </if>
            <if test="mdate != null and mdate != ''">
                mdate = #{mdate},
            </if>
            <if test="mstatus != null">
                mstatus = #{mstatus},
            </if>
            <if test="endaddress != null">
                endaddress = #{endaddress},
            </if>
            <if test="ps != null">
                ps = #{ps}
            </if>
        </set>
        where id = #{id}
    </update>

    <!-- 删除功能 -->
    <update id="deleteById">

        update  material set del = 0  where id = #{id}

    </update>



</mapper>