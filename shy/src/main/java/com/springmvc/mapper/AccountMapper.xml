<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.springmvc.mapper.AccountMapper" >

    <resultMap id="AccountMap" type="com.springmvc.entity.Account" >
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <id column="id" property="id" jdbcType="INTEGER" />
        <result column="ename" property="ename" jdbcType="VARCHAR" />
        <result column="position" property="position" jdbcType="VARCHAR" />
        <result column="wage" property="wage" jdbcType="OTHER" />
        <result column="enddate" property="enddate" jdbcType="TIMESTAMP" />
        <result column="wstatus" property="wstatus" jdbcType="INTEGER"/>
    </resultMap>


    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="AccountMap">
        select
            id,ename,position,wage,enddate,wstatus
        from account
        limit #{offset}, #{limit}

    </select>
    <!--查询全部用户订单信息-->
    <select id="queryAccountAll" resultMap="AccountMap">
        select
            *
        from account
        order by id
    </select>

    <!--查询全部用户订单信息-->
    <select id="queryAccountByInf" resultMap="AccountMap">
        select
        *
        from account
        <where>

            <if test="ename != null">
                ename = #{ename}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true" >
        insert into account(id,ename,position,wage,enddate,wstatus)
        values (#{id},#{ename}, #{position}, #{wage}, #{enddate}, #{wstatus})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update account
        <set>
            <if test="ename != null and ename != ''">
                ename = #{ename},
            </if>
            <if test="position != null ">
                position = #{position},
            </if>
            <if test="wage != null">
                wage = #{wage},
            </if>
            <if test="enddate != null">
                enddate = #{enddate},
            </if>
            <if test="wstatus != null">
                wstatus = #{wstatus},
            </if>

        </set>
        where id = #{id}
    </update>

    <!-- 删除功能 -->
    <delete id="deleteById">
        delete from account where id = #{id}
    </delete>

    
</mapper>