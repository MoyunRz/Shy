<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.springmvc.mapper.EmployessMapper" >


    <resultMap id="EmployeesMap" type="com.springmvc.entity.Employees" >
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <id column="id" property="id" jdbcType="INTEGER" />
        <result column="ename" property="ename" jdbcType="VARCHAR" />
        <result column="position" property="position" jdbcType="VARCHAR" />
        <result column="sex" property="sex" jdbcType="INTEGER" />
        <result column="age" property="age" jdbcType="INTEGER" />
        <result column="eaddress" property="eaddress" jdbcType="VARCHAR" />
        <result column="cdate" property="cdate" jdbcType="VARCHAR"/>
    </resultMap>


    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="EmployeesMap">
        select
            id,ename,position,sex,age,eaddress,cdate
        from employees
        limit #{offset}, #{limit}

    </select>
    <!--查询全部用户订单信息-->
    <select id="queryEmployeesAll" resultMap="EmployeesMap">
        select
            *
        from employees
        order by id 
    </select>

    <!--查询全部用户订单信息-->
    <select id="queryEmployeesByInf" resultMap="EmployeesMap">
        select
        *
        from employees
        <where>

            <if test="ename != null">
                ename = #{ename}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="addEmployees" keyProperty="id" useGeneratedKeys="true" >
        insert into employees( id,ename,position,sex,age,eaddress,cdate)
        values (#{id},#{ename}, #{position}, #{sex}, #{age}, #{eaddress},#{cdate})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update employees
        <set>
            <if test="ename != null and ename != ''">
                ename = #{ename},
            </if>
            <if test="position != null ">
                position = #{position},
            </if>
            <if test="sex != null">
                sex = #{sex},
            </if>
            <if test="age != null">
                age = #{age},
            </if>
            <if test="eaddress != null">
                eaddress = #{eaddress},
            </if>
            <if test="cdate != null">
                cdate = #{cdate},
            </if>
        </set>
        where id = #{id}
    </update>

    <!-- 删除功能 -->
    <delete id="deleteById">
        delete from employees where id = #{id}
    </delete>











</mapper>