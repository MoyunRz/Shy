<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.springmvc.mapper.LifeSpendMapper" >

    <resultMap id="LifeSpendMap" type="com.springmvc.entity.LifeSpend" >
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <id column="id" property="id" jdbcType="INTEGER" />
        <result column="useText" property="useText" jdbcType="VARCHAR" />
        <result column="sum" property="sum" jdbcType="OTHER" />
        <result column="spendtime" property="spendtime" jdbcType="TIMESTAMP" />
    </resultMap>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="LifeSpendMap">
        select
            id,useText,sum,spendtime
        from lifespend
        limit #{offset}, #{limit}

    </select>
    <!--查询全部用户订单信息-->
    <select id="queryLifeSpendAll" resultMap="LifeSpendMap">
        select
            *
        from lifespend
        order by id
    </select>

    <!--查询全部用户订单信息-->
    <select id="queryLifeSpendByInf" resultMap="LifeSpendMap">
        select
        *
        from lifespend
        <where>

            <if test="spendtime != null">
               spendtime = #{spendtime}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="addSpend" keyProperty="id" useGeneratedKeys="true" >
        insert into lifespend(id,useText,sum,spendtime)
        values (#{id},#{useText}, #{sum}, #{spendtime})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update lifespend
        <set>
            <if test="useText != null and useText != ''">
                useText = #{useText},
            </if>
            <if test="sum != null ">
                 sum = #{sum},
            </if>
            <if test="spendtime != null">
                 spendtime = #{spendtime},
            </if>
        </set>
        where id = #{id}
    </update>

    <!-- 删除功能 -->
    <delete id="deleteById">
        delete from lifeSpend where id = #{id}
    </delete>










</mapper>