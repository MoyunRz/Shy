<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.springmvc.mapper.EHolidayMapper" >

    <resultMap id="EHolidayMap" type="com.springmvc.entity.EHoliday" >
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <id column="id" property="id" jdbcType="INTEGER" />
        <result column="ename" property="ename" jdbcType="VARCHAR" />
        <result column="position" property="position" jdbcType="VARCHAR" />
        <result column="holiday" property="holiday" jdbcType="INTEGER" />
        <result column="sdate" property="sdate" jdbcType="TIMESTAMP"/>
        <result column="edate" property="edate" jdbcType="TIMESTAMP"/>
        <result column="ps" property="ps" jdbcType="VARCHAR"/>
        <result column="del" property="del" jdbcType="INTEGER"/>

    </resultMap>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="EHolidayMap">
        select
            id,ename,position,holiday,sdate,edate,ps
        from eholiday
        limit #{offset}, #{limit}

    </select>
    <!--查询全部用户订单信息-->
    <select id="queryEHolidayAll" resultMap="EHolidayMap">
        select
            *
        from eholiday
        where del=1
        order by id
    </select>

    <!--查询全部用户订单信息-->
    <select id="queryEHolidayByInf" resultMap="EHolidayMap">
        select
        *
        from eholiday
        <where>

            <if test="ename != null">
                ename = #{ename}
            </if>
            <if test="sdate != null">
                and sdate = #{sdate}
            </if>
            <if test="edate != null">
                and edate = #{edate}
            </if>
            and del=1
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true" >
        insert into eholiday(id,ename,position,holiday,sdate,edate,ps)
        values (#{id},#{ename}, #{position}, #{holiday}, #{sdate}, #{edate}, #{ps})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update eholiday
        <set>
            <if test="ename != null and ename != ''">
                ename = #{ename},
            </if>
            <if test="position != null ">
                position = #{position},
            </if>
            <if test="holiday != null">
                holiday = #{holiday},
            </if>
            <if test="sdate != null">
                sdate = #{sdate},
            </if>
            <if test="edate != null">
                edate = #{edate},
            </if>
            <if test="ps != null">
                ps = #{ps}
            </if>

        </set>
        where id = #{id}
    </update>

    <!-- 删除功能 -->
    <update id="deleteById">

        update eholiday set del = 0 where id = #{id}

    </update>


</mapper>